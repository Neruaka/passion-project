// WebSocket client for the real-time coach chat (US-003).
// Handles token streaming, reconnection with backoff, and typed messages
// (see API_CONTRACTS.md > CHAT > WS /ws/chat).
// TODO(sprint-3): implement connect, send, onMessage, auto-reconnect.

export class ChatSocket {
  // placeholder
}
