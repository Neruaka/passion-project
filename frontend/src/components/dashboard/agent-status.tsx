// Real-time agent status indicator (US-002): idle/thinking/executing/error/sleeping.
// Subscribes via WebSocket, falls back to polling. Animated transitions.
// TODO(sprint-3): implement with use-agent-status hook.

export function AgentStatus() {
  return null; // placeholder
}
